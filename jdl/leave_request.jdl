entity LeaveRequest {
    leaveDate Instant
    reason String
    status String
    approvedUserId Long
    approvedName String
    reply String

    userId Long

    createdDate Instant
    createdBy String
    lastModifiedDate Instant
    lastModifiedBy String
}

paginate * with pagination

dto * with mapstruct

service all with serviceImpl
